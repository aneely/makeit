# Handoff

Read `AGENTS.md`, `PLAN.md`. No active in-progress story.

Next task: pick up from `plans/todo/`.

Notes:
- `$MAKEIT_CONFIG` env var is retired; engine now reads `~/.config/makeit/sources`
- `actions/checkout@v4` Node.js 20 deprecation warning — bump before June 2026
