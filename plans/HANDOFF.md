# Handoff

`finder-tabs-smb` story is complete. A tabbed Finder window (~ and ~/Downloads) and a
separate SMB volume window now open as part of the morning profile. Helpers (`openFinderTabs`,
`mountVolume`, `openVolume`) live as local functions in the private profiles repo.

Next session: no story currently in progress. Candidates from `plans/todo/` are
`engine-error-passthrough.md` (profile error codes), `cold-start-validation.md` (real
restart test — dovetails with the SMB mount work just completed), or `lua-doc-comments.md`.
Read `AGENTS.md` and `PLAN.md` to orient.
